apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: text-editor-app-grafana
  namespace: text-editor-app
  labels:
    instance: text-editor-app
spec:
  route: {}
  config:
    log:
      mode: console
      level: warn
  deployment:
    spec:
      template:
        spec:
          containers:
            - name: grafana
              envFrom:
                - secretRef:
                    name: text-editor-app-grafana-admin-credentials
